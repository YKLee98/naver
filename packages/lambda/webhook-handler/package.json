{
  "name": "@hallyu/webhook-handler",
  "version": "1.0.0",
  "description": "Shopify webhook handler Lambda function",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild handler.ts --bundle --minify --sourcemap --platform=node --target=es2022 --outdir=dist",
    "package": "npm run build && cd dist && zip -r ../webhook-handler.zip .",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.490.0",
    "@aws-sdk/client-secrets-manager": "^3.490.0",
    "aws-lambda": "^1.0.7",
    "axios": "^1.6.5",
    "ioredis": "^5.3.2",
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.133",
    "esbuild": "^0.19.11"
  }
}

